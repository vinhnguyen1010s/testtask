{
  "name": "react-todo-app",
  "version": "1.0.0",
  "description": "json-server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run generator && node ./src/main.js",
    "dev": "npm run generator && nodemon ./src/main.js",
    "generator": "node ./src/generator.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Vinh Nguyen",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
